export enum ProviderCode {
	Unknow = 0,
	Google,
	Facebook,
}

export enum DevicePlatform {
    UNKNOW = 0,
    ANDROID,
    IOS,
	WEB,
} 

export enum UserRole {
	GUEST = 0,
	UNACTIVE,
	DEFAULT,
	ADMIN
}